fastapi>=0.110.0
uvicorn[standard]>=0.27.0
requests>=2.31.0
pydantic>=2.0.0

# --- Image generation ---
# All diffusion runs in the FLUX ComfyUI sidecar, which has its own venv with its
# own torch (see run.sh). The backend deliberately holds no torch/diffusers: it
# only reads and encodes image bytes, and drives ComfyUI over HTTP + websocket.
pillow>=10.0.0
websocket-client==1.8.0
# Used only by the model-download subprocess, to list a HF repo's files.
huggingface_hub==0.25.2
